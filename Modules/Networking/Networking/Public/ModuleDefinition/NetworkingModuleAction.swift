//
//  NetworkingModuleAction.swift
//  Networking
//

import Common

public enum NetworkingModuleAction: ModuleAction {
    /* e.g.
     case tokenExpired
     to notify the app and skip the entire responder chain from module's HTTP Layer.
     */
    // TODO: Fill this enum with all possible actions (cases) visible outside of the module which cannot be handled from within the module.
}
